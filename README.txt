EXPENSE TRACKER V9
Search/rendering fix.

Fixed:
- Transactions Search field no longer displays [object HTMLInputElement].
- Search, Type and Category filters use explicit element references and stable state names.
- New transaction type/category switching remains fixed.
- V9 has a new cache version.

Update:
Replace ALL files in your GitHub Pages repository with these files, commit, wait 1-2 minutes, then open the GitHub Pages URL in Safari.
Do not clear Safari website data.
